includes:
    - vendor/phpstan/phpstan-mockery/extension.neon

parameters:
    level: max

    paths:
        - src

    stubFiles: []

    reportUnmatchedIgnoredErrors: false

    ignoreErrors:
        - message: '#.*PHPUnit.*TestCase.*#'